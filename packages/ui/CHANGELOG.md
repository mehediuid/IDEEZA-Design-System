# @ideeza/ui

## 0.2.0

### Minor Changes

- bdeca59: Add the first four components mirroring the Figma atoms: `Button` (A01 — primary/secondary/ghost/danger × sm–2xl, loading, icons, focus ring), `IconButton` (A02), `Badge` (A17 — subtle/solid/outline × 6 colors × 3 sizes, dot, dismissible), and `Toggle` (A10, Radix Switch). All styling uses semantic tokens from `@ideeza/tokens`.
