# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.turbo/
storybook-static/
*.tsbuildinfo

# Coverage
coverage/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*.local
preview/
package-lock.json
ideeza-color-changes.pdf
.deps-platform

# npm pack output — a build artifact, and one that would carry a stale copy
# of dist into the repo if it were ever committed.
*.tgz

# npm auth token. Never commit this; publishing uses it and it grants
# publish rights to every package the account owns.
.npmrc
