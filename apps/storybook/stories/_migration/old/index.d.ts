// The vendored 0.2.0 build, typed loosely on purpose: this only has to render
// next to the new one for a computed-style diff, and it disappears with the
// rest of `_migration/` once every component has moved.
import type * as React from "react";
export declare const Button: React.ComponentType<Record<string, unknown>>;
